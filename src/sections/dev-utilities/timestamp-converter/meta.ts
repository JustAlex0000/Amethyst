import type { ModuleMeta } from "@/lib/registry";

const meta: ModuleMeta = {
  id: "timestamp-converter",
  section: "dev-utilities",
  name: "Timestamp Converter",
  description: "Convert between unix timestamps, ISO 8601 and human-readable dates across timezones.",
  keywords: ["unix", "timestamp", "timezone", "iso", "date", "epoch"],
};

export default meta;
