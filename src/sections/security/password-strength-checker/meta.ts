import type { ModuleMeta } from "@/lib/registry";

const meta: ModuleMeta = {
  id: "password-strength-checker",
  section: "security",
  name: "Password Strength Checker",
  description: "Estimate password entropy and spot weak patterns. Runs entirely in your browser.",
  keywords: ["password", "entropy", "strength", "security", "breach"],
};

export default meta;
