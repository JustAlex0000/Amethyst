import type { ModuleMeta } from "@/lib/registry";

const meta: ModuleMeta = {
  id: "timestamp-generator",
  section: "discord",
  name: "Timestamp Generator",
  description: "Generate Discord <t:unix:FORMAT> timestamps with live previews of each format.",
  keywords: ["discord", "timestamp", "unix", "relative", "time"],
};

export default meta;
