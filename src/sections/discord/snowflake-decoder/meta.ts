import type { ModuleMeta } from "@/lib/registry";

const meta: ModuleMeta = {
  id: "snowflake-decoder",
  section: "discord",
  name: "Snowflake Decoder",
  description: "Extract the creation timestamp and worker bits from any Discord ID.",
  keywords: ["discord", "snowflake", "id", "timestamp", "decode"],
};

export default meta;
