import type { ModuleMeta } from "@/lib/registry";

const meta: ModuleMeta = {
  id: "embed-builder",
  section: "discord",
  name: "Embed Builder",
  description: "Build Discord embeds visually and copy the JSON payload.",
  keywords: ["discord", "embed", "json", "webhook", "message"],
};

export default meta;
