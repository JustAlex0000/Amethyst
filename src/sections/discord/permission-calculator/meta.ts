import type { ModuleMeta } from "@/lib/registry";

const meta: ModuleMeta = {
  id: "permission-calculator",
  section: "discord",
  name: "Permission Calculator",
  description: "Convert between a Discord permission bitfield and a checkbox list, and build an OAuth invite link from it.",
  keywords: ["discord", "permissions", "bitfield", "calculator", "bot", "invite", "oauth", "url", "scopes"],
};

export default meta;
