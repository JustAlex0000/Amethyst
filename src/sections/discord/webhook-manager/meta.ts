import type { ModuleMeta } from "@/lib/registry";

const meta: ModuleMeta = {
  id: "webhook-manager",
  section: "discord",
  name: "Webhook Manager",
  description: "Compose, send, edit and delete Discord webhook messages, with username and avatar overrides.",
  keywords: ["discord", "webhook", "send", "edit", "delete", "message", "avatar", "username"],
};

export default meta;
