import type { ModuleMeta } from "@/lib/registry";

const meta: ModuleMeta = {
  id: "text-editor",
  section: "text-editor",
  name: "Text Editor",
  description: "Write and edit text with markdown preview, diff, regex, case conversion, AI-style and injection analysis.",
  keywords: ["markdown", "diff", "regex", "case", "word count", "editor", "write", "ai", "stylometry", "prompt injection", "scan"],
};

export default meta;
